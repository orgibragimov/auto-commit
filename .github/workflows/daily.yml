name: Daily 100 Commits

on:
  schedule:
    - cron: "0 0 * * *"   # Har kuni UTC 00:00
  workflow_dispatch:

permissions:
  contents: write

jobs:
  auto-commit:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          ref: main   # agar default branch master bo‘lsa → master

      - name: Setup Git
        run: |
          git config --global user.name "orgibragimov"
          git config --global user.email "orgibragimov@users.noreply.github.com"

      - name: Ensure file exists
        run: touch activity-log.txt

      - name: Make 100 guaranteed commits
        env:
          GH_PAT: ${{ secrets.GH_PAT }}
        run: |
          for i in $(seq 1 100); do
            # Har commit noyob bo‘lishi uchun nanosekund qo‘shamiz
            echo "$(date +%s%N) commit $i" >> activity-log.txt
            git add activity-log.txt
            git commit -m "Auto commit $i ($(date +%F %T))" || echo "No changes"
          done

      - name: Push commits
        run: git push https://$GH_PAT@github.com/orgibragimov/auto-commit.git HEAD:main
